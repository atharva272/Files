slip 19

1.
// ----- PRODUCT -----
abstract class Pizza {
    String name;

    void prepare() {
        System.out.println("Preparing " + name);
    }

    void bake() {
        System.out.println("Baking " + name);
    }

    void cut() {
        System.out.println("Cutting " + name);
    }

    void box() {
        System.out.println("Boxing " + name);
    }

    public String getName() {
        return name;
    }
}

// ----- CONCRETE PRODUCTS -----
class NYStyleCheesePizza extends Pizza {
    NYStyleCheesePizza() {
        name = "NY Style Cheese Pizza";
    }
}

class ChicagoStyleCheesePizza extends Pizza {
    ChicagoStyleCheesePizza() {
        name = "Chicago Style Cheese Pizza";
    }

    @Override
    void cut() {
        System.out.println("Cutting Chicago pizza into SQUARE slices");
    }
}

// ----- CREATOR (Factory Method) -----
abstract class PizzaStore {

    public Pizza orderPizza(String type) {
        Pizza pizza = createPizza(type); // factory method

        pizza.prepare();
        pizza.bake();
        pizza.cut();
        pizza.box();

        return pizza;
    }

    // Factory Method (implemented by subclasses)
    abstract Pizza createPizza(String type);
}

// ----- CONCRETE CREATORS -----
class NYPizzaStore extends PizzaStore {
    Pizza createPizza(String type) {
        if (type.equalsIgnoreCase("cheese"))
            return new NYStyleCheesePizza();
        return null;
    }
}

class ChicagoPizzaStore extends PizzaStore {
    Pizza createPizza(String type) {
        if (type.equalsIgnoreCase("cheese"))
            return new ChicagoStyleCheesePizza();
        return null;
    }
}

// ----- MAIN -----
public class Main {
    public static void main(String[] args) {

        PizzaStore nyStore = new NYPizzaStore();
        PizzaStore chicagoStore = new ChicagoPizzaStore();

        System.out.println("---- Ordering NY Cheese Pizza ----");
        nyStore.orderPizza("cheese");

        System.out.println("\n---- Ordering Chicago Cheese Pizza ----");
        chicagoStore.orderPizza("cheese");
    }
}


2.
✅ Q.2 Start Raspberry Pi and Execute Linux Commands – Simple Steps
1️⃣ How to Start Raspberry Pi

Connect HDMI cable, keyboard, mouse, and power cable.

Insert SD card with Raspberry Pi OS.

Power ON → Raspberry Pi will boot.

You will see the desktop.

Click on Terminal (black screen icon).

Now you can type Linux commands.

✅ 2️⃣ Basic Linux Commands (Very Simple Explanation)

I am giving each command with
✔ syntax
✔ meaning
✔ example

📌 1. ls – List files

Shows files/folders in the current directory.

ls

📌 2. cd – Change Directory

Go inside another folder.

cd folder_name


Go back:

cd ..

📌 3. touch – Create empty file
touch myfile.txt

📌 4. mkdir – Make directory
mkdir myfolder

📌 5. rmdir – Remove empty directory
rmdir myfolder

📌 6. rm – Remove file
rm myfile.txt


Remove folder with files:

rm -r folder_name

📌 7. mv – Move or rename

Rename file:

mv oldname.txt newname.txt


Move file:

mv file.txt /home/pi/Documents/

📌 8. cat – View file content
cat file.txt

📌 9. more – View large file page-by-page
more file.txt

📌 10. less – Advanced file viewer
less file.txt


(Press q to exit)

📌 11. man – Manual help

Shows help for any command.

man ls


(exit with q)

📌 12. tar – Create/extract archive (zip)

Create tar archive:

tar -cvf files.tar folder/


Extract:

tar -xvf files.tar

📌 13. gzip – Compress file

Compress:

gzip file.txt


Decompress:

gunzip file.txt.gz

📌 14. ps – Show running processes
ps

📌 15. sudo – Run command as admin
sudo apt update

📌 16. cron – Schedule tasks

Open cron table:

crontab -e


Example schedule (run every minute):

* * * * * echo "Hello" >> test.txt

📌 17. chown – Change file owner
sudo chown pi file.txt

📌 18. chgrp – Change group
sudo chgrp users file.txt

📌 19. ping – Check network
ping google.com


Stop: Ctrl + C

✅ 3️⃣ Final Practical Output Format (Write This in File)
Start Raspberry Pi → Open Terminal → Execute following commands:

ls      : list directory files
cd      : change directory
touch   : create file
mkdir   : create folder
rmdir   : remove empty folder
rm      : remove file or folder
mv      : move or rename file
cat     : show file content
more    : view page-wise
less    : advanced page viewer
man     : show help manual
tar     : create/extract archive
gzip    : compress/decompress file
ps      : show running processes
sudo    : run as super user
cron    : schedule tasks
chown   : change owner
chgrp   : change group
ping    : check network connection